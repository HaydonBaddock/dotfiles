(defwidget updates []
  (box :class "subtle"
       :visible {updates > 0}
       :spacing 8
       :space-evenly false
    (label :class "icon"
           :text "󰚰")
    (label :text updates)))

(defpoll updates :interval "1h" :initial "0"
  "checkupdates | wc -l")
