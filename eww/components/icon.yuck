(defwidget icon [char y size ?visible]
  (overlay :visible visible
    (label :class "invisible"
           :text ".......")
    (transform :translate-y y
      (label :style "font-size: ${size}"
             :text char))))
